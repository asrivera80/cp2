apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  selector:
    app.kubernete.io/name: nginx
  ports:
    - name: http
    #- protocol: TCP
      port: 80
      nodePort: 31811
  type: LoadBalancer